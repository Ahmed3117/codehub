
i need to make the app works into 3 modes as following :
1 - firsr modde is the current mode as our app works now exactly without any change 
2 - the second mode is focus mode , this mode we select some sessions to focus on them , so other session will directly be disabled untill we change the mode , also we can select multible focus periods , i mean we can save a period  from time to time to work with some sessions (select the time from and to and select the sessions for the period) so directly when the time of a period starts , enable this period directly and disable other sessions automatically (we can also save periods to run daily or to run from a date to another (exaple : from 11-may-2026 to 25-may-2026) , we can execlude days too ) 
3 - the third mode is mangaed mode , i mean to connect it to the pomodoro as follong :
 - we will select some sessions to work with .
 - every session of those we selected will be activated and selected as ready to work on for  a pomodoro work duration , then when this duration finished , the next session of the selected ones will automatically selected and activated to be ready to work on . (on breaks , the all app will be disabled , so nothing at all will work on breaks , so i can enjoy my break).

note : the third mode can be activated alone or can be activated with the second mode in the same time , so i can make use of the focus periods mode and the management mode in the same time (in this case we will not need to select sessions for the management mode but it will directly work on the active period sessions and it will be dynamically change the sessions based on the active period)
