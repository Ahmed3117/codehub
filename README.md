# codehub
# 1. Change to the project directory
cd /media/issa1/projects/mine/personal/main/coder3

# 2. Install using dpkg (the most reliable way for local .deb files)
sudo dpkg -i ./codehub_0.1.1_amd64.deb

# 3. If there are any missing dependency errors, fix them with:
sudo apt install -f
