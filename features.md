1 - reordering the apps of the session pannel not working 
2 - rename an app of the session pannel opend apps not working 
3 - the side bar takes a big part of the screen so decrease its width
4 - on adding or edit a session , we select an editor , lets make this optional , so i need the session to general to be more dynamically used , so i can use it in programming o another things , so the session can be empty without editor or and without project path (they can be optional) 
5 - add option to replace an editor for a session , so when i click 'replace the editor' , i can select another editor to open the project path of this session and kills the editor that wa sed for it before .